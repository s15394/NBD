printjson(db.people.mapReduce(     function() {  emit( this.sex, {weight: this.weight, height: this.height})  },          function(key, values) {          w = 0; h = 0;         for (let i = 0; i< values.length; i++) {             w+= values[i].weight;             h+= values[i].height;         }         return {weight: w/values.length, height: h/values.length}},      { out: "people_avg_weight_avg_height_per_sex"} ).find().toArray())
